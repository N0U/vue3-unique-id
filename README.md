# vue3-unique-id

Vue3 plugin to generate unique ids for HTML identifiers for use in form inputs, labels and so on.   
Inspired by [vue-unique-id](https://github.com/snoozbuster/vue-unique-id). Somehow the original lib doesn't work with Vue3 anymore, so I created my own.

